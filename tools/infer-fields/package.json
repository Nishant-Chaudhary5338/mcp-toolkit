{
  "name": "@mcp-showcase/infer-fields",
  "version": "1.0.0",
  "description": "Infer a typed FieldSchema (fields, types, relations, table/form defaults) from a JSON API sample or OpenAPI schema — the data contract every CRUD/form/table generator consumes.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
