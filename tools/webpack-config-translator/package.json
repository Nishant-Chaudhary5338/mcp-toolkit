{
  "name": "@mcp-showcase/webpack-config-translator",
  "version": "1.0.0",
  "description": "Best-effort translation of a webpack/CRACO config into Vite equivalents — aliases, plugin/loader classification (native/needs-plugin/unsupported), and an explicit manual-review list.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
