{
  "name": "@mcp-showcase/codemod-runner",
  "version": "1.0.0",
  "description": "A generic rule-based codemod engine — apply a find/replace (regex with backrefs) or a named built-in migration across files, dry-run by default with rollback via the returned diff.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
