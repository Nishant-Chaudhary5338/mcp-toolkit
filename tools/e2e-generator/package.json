{
  "name": "@mcp-showcase/e2e-generator",
  "version": "1.0.0",
  "description": "Generate a Playwright end-to-end CRUD flow spec (create->list->edit->delete + a11y) from a FieldSchema — tests the feature the CRUD factory generates.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
