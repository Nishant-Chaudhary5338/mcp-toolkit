{
  "name": "@mcp-showcase/docs-generator",
  "version": "1.0.0",
  "description": "Generate documentation from source — a README for an MCP tool (from its addTool registrations) or an API reference for a TS module (from its exported functions/types + JSDoc).",
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
