{
  "name": "@mcp-showcase/mcp-tool-improviser",
  "version": "2.0.0",
  "description": "Analyze and improve MCP tools across 7 dimensions (description quality, schema completeness, error handling, edge cases, response structure, code quality, contextual depth) with proposed diffs, apply, and rollback.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
