{
  "name": "@mcp-showcase/refactor-executor",
  "version": "1.0.0",
  "description": "Safely execute refactor plans on React/TS codebases — move/rename files, split modules, update all imports, create barrels, validate the build, and roll back on failure.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@typescript-eslint/parser": "^8.0.0",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.10"
  },
  "devDependencies": { "@types/node": "^20.0.0", "@types/fs-extra": "^11.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
