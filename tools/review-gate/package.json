{
  "name": "@mcp-showcase/review-gate",
  "version": "1.0.0",
  "description": "Grade generated or changed React/TS code A–F — a blocking quality gate: typecheck signal, a11y (missing alt, unlabeled controls), design tokens (hardcoded hex colors), and code smells (any, console.log).",
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
