{
  "name": "@mcp-showcase/jest-to-vitest-migrator",
  "version": "1.0.0",
  "description": "Migrate Jest test files to Vitest — jest.* -> vi.* (with requireActual->importActual), add the vitest import, and flag vi.mock factories for manual review.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
