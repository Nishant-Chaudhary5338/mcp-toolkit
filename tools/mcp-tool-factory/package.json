{
  "name": "@mcp-showcase/mcp-tool-factory",
  "version": "1.0.0",
  "description": "Scaffold, wire, and verify new MCP tools inside the mcp-react-toolkit package — the executable form of the mcp-server-builder skill. Kills the boilerplate; leaves only the logic.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
