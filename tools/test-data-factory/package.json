{
  "name": "@mcp-showcase/test-data-factory",
  "version": "1.0.0",
  "description": "Generate a typed test-fixture factory (makeX/makeXs with overrides) from a FieldSchema — deterministic mock data for unit tests, Storybook, and seeds.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
