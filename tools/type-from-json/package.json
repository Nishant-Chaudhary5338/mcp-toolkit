{
  "name": "@mcp-showcase/type-from-json",
  "version": "1.0.0",
  "description": "Generate plain TypeScript interfaces from a JSON sample (nested objects become their own interfaces) — the general-purpose sibling of infer-fields.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
