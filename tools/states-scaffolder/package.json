{
  "name": "@mcp-showcase/states-scaffolder",
  "version": "1.0.0",
  "description": "Generate loading-skeleton, empty-state, and error-state components plus a switch wrapper for a data view — kills the copy-pasted conditional-render boilerplate.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
