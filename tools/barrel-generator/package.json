{
  "name": "@mcp-showcase/barrel-generator",
  "version": "1.0.0",
  "description": "Generate an index.ts barrel that re-exports every module in a folder — kills hand-maintained export lists that drift out of sync.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
