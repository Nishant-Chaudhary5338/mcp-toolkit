{
  "name": "@mcp-showcase/env-config-generator",
  "version": "1.0.0",
  "description": "Generate a Zod-validated, typed env module (Vite or Next) from env var names or an .env.example — throws on boot with a readable error.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": { "build": "tsc && chmod +x build/index.js", "dev": "tsc --watch", "test": "vitest run" },
  "dependencies": { "@mcp-showcase/shared": "*", "@modelcontextprotocol/sdk": "^1.12.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.0.0", "vitest": "^2.0.0" }
}
