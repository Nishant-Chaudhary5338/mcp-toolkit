{
  "name": "@mcp-showcase/zod-schema-generator",
  "version": "1.0.0",
  "description": "Generate a Zod schema and inferred TypeScript type from a FieldSchema — Zod at every boundary for CRUD forms and API clients.",
  "type": "module",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-showcase/shared": "*",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
